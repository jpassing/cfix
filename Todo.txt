cfixcc_log
cfixcc_assert_Equals_String

General
-------
test coverage
prefast

1.3
---
CFIXCC_ASSERT_EQUALS_STRING?
 - single-testcase runs
 - prettier CUI
 - IAT hooking
  + C++ API (RAII)
  - threading
  
 - XML testrun definitions
 
  
 
Cfix IAT Instrumentation
------------------------
 CiiHook: place dynamically generated thunk in IAT:
  call CiipCallIatRoutine( <original_va> )
    - check thread-local HT for hook
    - else jmp to <original_va>
 lock for instrumenting, calls lock-free
  -> IAT-replacement atomic
  
  
  - call invokations per thread
    OR: specify pre-invokation hook when hooking --> place in HT
  - RevokeAllOfCurrentThread
  
  
  - UM, KM?
 
 
BUGS
----
 TestResolveMessages not platform-neutral -> Win2000


TODO
----
Log Severities?
Job object
-u default if debugger attached



Doc
---
 * Using in the debugger
   * use -u
   * set working dir 
 * mem leak detection 
   --> different CRTS
   --> UMDH: -y
 * Feedback

 * Avrf
 * DEP
 
 * Logging
 
 * prefast in makefile
	remove -f
		- CFIX_E_TESTRUN_ABORTED
		- CfixAbort
  
 * CfixCreateCrtThread 
 
Parallel
--------
 TsExecAction:
  - flag: all/run setup only/run teardown only/run tests only
  -> called by ParallelAction, make sure setup/teardown called only once, while tests can run in parallel
 CFIX_FIXTURE_OPTIONS( SUPPORTS_MT )
   
Future Features
---------------
 * AssertEqualsDword -> optionally resolve values as error messages
 * pass additional message DLLs on cmdline
 * (optionally) run each fixture in separate process -> robustness
 * Dbg: choose ignore bp/abort TC
 
 * filter top frames of stack traces 
 
 preferred load address?
 
 Show ODS-message when invalid cmdine params passed
 
 Leak detection (Heap*) based on jpfbt-tracing
 
Bugs
----
 cdiag: Resolver: try clearing NT-flag

Todo
----
 * Stacktrace Symbol Resolving KM

 
Known Issues
------------
 - No stacktrace in KM for unhandled exceptions
 
